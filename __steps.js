/**
 * ------------------
 * ONE TIME PROCESS
 * ------------------
 * 1. create and account.
 * 2. verify your email.
 * 3. install heroku cli
 * 4. heroku login
 * --------------------
 * EACH PROJECT PROCESS
 * --------------------
 * 1. Open your project via cmd
 * 2. heroku create
 * 3. git add. git commit, git push
 * 4. git push heroku main
 * 5. go to heroku dashboard > current project> setting> reveal config vars
 * 6. copy past .env file
 * 7. make sure you have whitelisted all address to access mongodb
 *  -------------------------
 *  UPDATE SERVER WITH NEW CHANGES
 * ----------------------------------
 * 1. Make changes
 * 2. git add. git commit, git push
 * 3. git push heroku main
 * ----------------------
 *  FOR CLINT SIDE
 * ----------------------
 * 1. replace localhost by heroku link
 * 2. npm run build
 * 3. firebase deploy
 */
